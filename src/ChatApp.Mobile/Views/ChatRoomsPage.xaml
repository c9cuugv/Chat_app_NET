<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ChatApp.Mobile.ViewModels"
             xmlns:model="clr-namespace:ChatApp.Core.Entities;assembly=ChatApp.Core"
             x:Class="ChatApp.Mobile.Views.ChatRoomsPage"
             Title="{Binding Title}">

    <RefreshView Command="{Binding GetRoomsCommand}" IsRefreshing="{Binding IsBusy}">
        <CollectionView ItemsSource="{Binding Rooms}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:ChatRoom">
                    <Grid Padding="10">
                        <Frame HasShadow="True" BorderColor="Gray" CornerRadius="5">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ChatRoomsViewModel}}, Path=GoToChatCommand}" CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Type}" FontSize="12" TextColor="Gray" />
                            </VerticalStackLayout>
                        </Frame>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>
